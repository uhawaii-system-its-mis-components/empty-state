# empty-state

Start a development environment with:

```
make
```
Then, the first time:

```
yarn install
polymer serve --hostname 0.0.0.0
```

subsequently just run:

```
polymer serve --hostname 0.0.0.0
```
